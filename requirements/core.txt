# requirements/core.txt
# ──────────────────────────────────────────────────────────────
# Minimal runtime. Install first. Everything else is optional.
# pip install -r requirements/core.txt
# ──────────────────────────────────────────────────────────────

# Web server
fastapi>=0.136.3,<1.0
starlette>=1.0.1
uvicorn[standard]>=0.30,<1.0
websockets>=12.0
python-multipart>=0.0.9
aiofiles>=23.0

# Data validation
pydantic>=2.7,<3.0
pydantic-settings>=2.4

# HTTP client
httpx>=0.27
requests>=2.32
google-genai>=1.0.0
idna>=3.15
urllib3>=2.7.0
aiohttp>=3.9

# Config / serialisation
PyYAML>=6.0
orjson>=3.9

# Local AI inference
ollama>=0.3

# System & utilities
psutil>=5.9
structlog>=24.1
backoff>=2.2
colorlog>=6.0
prometheus-client>=0.21
python-dateutil>=2.9
PyYAML>=6.0

# Memory / storage
# (chromadb in ml.txt — core uses SQLite only)
aiosqlite>=0.20
sqlalchemy>=2.0.35
celery>=5.4
redis>=5.0
