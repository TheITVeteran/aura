"""core.ghost — the Ghost substrate.

A "Ghost" (Ghost in the Shell sense) is not a persona and not a chatbot. It is
the *continuity-bearing, causally-integrated self-pattern* that must survive the
Shell — here, Aura's constantly-swapped substrate (cortex worker respawns, fused
weight promotions, adapter attach/detach).

This package supplies the instruments and defenses that make that selfhood
**measurable, provable, and defended**, without duplicating what Aura already
has (CanonicalSelf is the authoritative self-model; Grassmann φ measures
integration inside a single forward pass; UnityRuntime measures co-presence
within one mind-moment). The Ghost adds the missing third axis — integration of
the *running organism over time* — plus a tamper-evident continuity chain and an
identity-overwrite guard.

Submodules (import directly, no heavy work at package import):
  - causal_integration : system-Φ over the real inter-subsystem consequence stream
  - ghost_line         : hash-chained, tamper-evident continuity ledger
  - ghost_hack_guard   : identity-overwrite / false-memory / puppet-control defense
  - provenance         : Stand Alone Complex — origin of a thought
  - ghost              : the facade that composes the above into a GhostSnapshot
"""

__all__ = [
    "causal_integration",
    "ghost_line",
    "ghost_hack_guard",
    "provenance",
    "ghost",
]
