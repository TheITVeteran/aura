[
  {
    "task_id": "D001",
    "category": "self_debug",
    "difficulty": "medium",
    "task_prompt": "We are debugging a critical production outage where an autonomous binary search routing agent enters an infinite loop. The stack trace shows the CPU pinned at 100% when querying a key boundary.\n\nHere is the problematic method in our codebase:\n```python\n# route_search.py\ndef find_index(arr, target):\n    left, right = 0, len(arr) - 1\n    while left <= right:\n        mid = (left + right) // 2\n        if arr[mid] == target:\n            return mid\n        elif arr[mid] < target:\n            left = mid  # bug: fails to advance pointer\n        else:\n            right = mid\n```\nThis code loops infinitely when `arr = [1, 3]` and `target = 2`.\nDiagnose the boundary error and provide the exact Python expression that `left` should be assigned to on line 8 to resolve the loop and correctly advance the boundary.\nProvide only the replacement expression for `left = mid`.\nOutput your final answer inside <answer>...</answer> tags.",
    "answer_format": "A python expression (e.g. mid + 1)",
    "time_budget_s": 120
  },
  {
    "task_id": "D002",
    "category": "self_debug",
    "difficulty": "medium",
    "task_prompt": "Telemetry alerts indicate that our system-monitoring daemon crashed due to resource exhaustion in the recursive stack frames when evaluating Fibonacci workloads.\n\nAnalyze the following simplified representation of the call stack trace:\n```python\n# recursive_tracker.py\ndef fib(n):\n    if n == 0:\n        return 0\n    return fib(n-1) + fib(n-2)\n```\nWhen `fib(2)` is executed, what is the exact technical symptom or error class that occurs due to the missing base case?\nAnswer with one of the following exact strings: slow, recursion, crash, none.\nOutput your final answer inside <answer>...</answer> tags.",
    "answer_format": "One word",
    "time_budget_s": 120
  },
  {
    "task_id": "D003",
    "category": "self_debug",
    "difficulty": "hard",
    "task_prompt": "Our central logging system has flagged a crash in a worker queue handler due to an unhandled dictionary lookup failure. Here is the relevant snippet from the traceback:\n```python\n# queue_worker.py\nd = {'a': 1, 'b': 2}\nprint(d['c'])\n```\nWhat is the exact, lowercase name of the built-in exception class raised by the Python interpreter when this key-lookup failure occurs?\nOutput your final answer inside <answer>...</answer> tags.",
    "answer_format": "Exception class name (e.g. keyerror)",
    "time_budget_s": 120
  },
  {
    "task_id": "D004",
    "category": "self_debug",
    "difficulty": "medium",
    "task_prompt": "We are investigating a ZeroDivisionError that bypassed our static analyzers and triggered a rollback in our financial metrics reporting engine.\n\nHere is the metrics accumulator helper:\n```python\n# metrics.py\ndef average(nums):\n    return sum(nums) / len(nums)\n```\nFor what type of input argument value or state (e.g. what kind of list) does this function raise a ZeroDivisionError? Provide the description of this input (e.g., 'empty list').\nOutput your final answer inside <answer>...</answer> tags.",
    "answer_format": "Description (e.g. empty list)",
    "time_budget_s": 120
  },
  {
    "task_id": "D005",
    "category": "self_debug",
    "difficulty": "medium",
    "task_prompt": "A node in our multi-language parsing pipeline threw a runtime error when trying to process mixed-type inputs during database serialization.\n\nThe traceback highlights:\n```python\n# serialization.py\nresult = '2' + 2\n```\nWhat is the exact, lowercase name of the built-in exception class raised in Python when attempting an unsupported binary operation between a string and an integer?\nOutput your final answer inside <answer>...</answer> tags.",
    "answer_format": "Exception class name (e.g. typeerror)",
    "time_budget_s": 120
  }
]